DATABASE_URL="postgresql://pm_lite:pm_lite_dev@localhost:5432/pm_lite"
JWT_SECRET="change-me-in-production-use-long-random-string"
PORT=3000

# Email (MailHog в dev, настоящий SMTP в prod)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="PM Lite <noreply@pm-lite.app>"

# URL фронтенда (для ссылок в письмах)
APP_URL="http://localhost:5173"

# Облачное хранилище файлов документов (режим cloud)
UPLOADS_DIR=./uploads

# Anthropic API (для AI-функций)
# ANTHROPIC_API_KEY=
